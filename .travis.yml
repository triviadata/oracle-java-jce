---
sudo: false

language: python

services: docker

install:
  - pip install ansible==2.7.7
  - pip install testinfra molecule docker
  - git clone --depth=50 --branch=master https://github.com/${TRAVIS_REPO_SLUG}.git triviadata.oracle-java-jce
  - cd triviadata.oracle-java-jce

script:
  - molecule test
